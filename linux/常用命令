1. tail


2. rm

3. su

4. kill

5. ps

6. rz 运行该命令会弹出一个文件选择窗口，从本地选择文件上传到服务器(receive)
rz是便于Linux/Unix同Windows进行ZModem文件传输的命令行工具，windows端需要支持ZModem的telnet/ssh客户端

7. sz 将选定的文件发送（send）到本地机器
rz/sz 通过Zmodem协议传输数据。